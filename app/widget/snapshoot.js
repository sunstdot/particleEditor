/**
 * Created by sunshitao on 2016/5/13.
 */
let samplingTime = 1000;
let record = false;
let recordData = {};

function sampling(target){
    var name = target.name;
    var pos = target.pos;
    if(recordData[name])
        recordData[name] = {}

}